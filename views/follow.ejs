<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>INSTA HOME </title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div>
        <h1>username :<%= user.username %> </h1>
        <h1>Description :<%= user.description %> </h1>
        
        <a href="http://localhost:8080/home/<%= user.username %>/newpost"> New Post </a>  
        
        <% for( let po of user.post ) { %>
        <div>
            <h2>caption : <%= po.caption %></h2>
            <h2>location : <%= po.location %></h2>
            <button> like : <%= po.like %>  </button>
            <button> comment : <%= po.comment %>  </button>

        </div>
        <% } %>
    </div>

</body>
</html>

